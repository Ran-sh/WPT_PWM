# 字库扩展 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 V4.3.1 基础上新增: 开机动画 (SPLASH 分区 5 帧 fade-in)、20 个功能图标 (电池/警告/箭头/齿轮等)、12×12 CJK 小字库 (全字库 612KB)

**Architecture:** B→A→C 顺序渐进实施。B 最小改动 (640B 图标数据追加到 `generate_font.py` ICON_SPEC + STM32 片内 ROM 回退字模)，A 独立 SPLASH 分区不影响字库，C 最大改动 (612KB 新字模 + 新解码逻辑)。

**Tech Stack:** Python 3.10+ / Pillow / flashrom 1.4 / ARMCC V5 C89 / SPL V3.5.0 / W25Q128

---

## 文件结构总览

| 文件 | 操作 | 子项目 | 行数变化 |
|:---|:---|:---|:---|
| `ch341/generate_font.py` | **修改** (追加 icon 20个 + 12×12 字模生成) | B, C | +80 |
| `ch341/generate_splash.py` | **新建** | A | ~80 |
| `ch341/burn_splash.py` | **新建** | A | ~60 |
| `Keil_Project/Hardware/TFT_Font_Data.h` | **修改** (追加 icon bitmaps) | B | +640 |
| `Keil_Project/Hardware/Tft_Driver.h` | **修改** (新 API: icon_id 查表 + 12×12 显示) | B, C | +15 |
| `Keil_Project/Hardware/Tft_Driver.c` | **修改** (12×12 解码 + icon Flash 查表) | B, C | +60 |
| `Keil_Project/User/Sys_Core.c` | **修改** (Sys_Startup_Screen 改为 splash 动画) | A | +30 |
| `Keil_Project/Hardware/W25Q_Driver.h` | **修改** (SPLASH magic + Font_Header v2 扩展) | A, C | +10 |
| `ch341/README.md` | **修改** (更新操作指南) | ALL | +20 |

---

### Task 1: B — 新增 20 个图标到 Python 端

**Files:**
- Modify: `ch341/generate_font.py` (ICON_SPEC 列表追加 20 条新图标)

- [ ] **Step 1: 在 `generate_font.py` ICON_SPEC 列表末尾追加 20 个新图标**

定位到 `ICON_SPEC = [` 列表末尾（ROCKET_ANIM 定义之后，`]` 之前），追加 20 个 16×16 位图:

```python
    # ── V4.3.1 新增图标: ID 11~30 (20个 16×16 功能图标) ──
    (11, "ICON_BATTERY", 1, bytes([
        0x00,0x00,0x00,0x00,0xFE,0x3F,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,
        0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0xFE,0x3F,0x00,0x00,0x00,0x00,
    ])),
    (12, "ICON_WARNING", 1, bytes([
        0x00,0x00,0x00,0x00,0x80,0x00,0xC0,0x01,0x40,0x01,0x60,0x03,0x20,0x02,0x30,0x06,
        0x10,0x04,0x18,0x0C,0x10,0x04,0x20,0x02,0x20,0x02,0xC0,0x01,0x80,0x00,0x00,0x00,
    ])),
    (13, "ICON_CHECK", 1, bytes([
        0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x40,0x80,0x20,
        0xC0,0x11,0x60,0x0B,0x30,0x06,0x18,0x0C,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
    ])),
    (14, "ICON_CROSS", 1, bytes([
        0x00,0x00,0x00,0x00,0x00,0x00,0x02,0x20,0x06,0x30,0x0C,0x18,0x18,0x0C,0x30,0x06,
        0x60,0x03,0xC0,0x01,0xC0,0x01,0x60,0x03,0x30,0x06,0x18,0x0C,0x00,0x00,0x00,0x00,
    ])),
    (15, "ICON_POWER", 1, bytes([
        0x00,0x00,0x80,0x00,0x80,0x00,0x80,0x00,0x80,0x08,0x80,0x04,0x80,0x02,0x80,0x01,
        0x00,0x01,0x00,0x02,0x00,0x04,0x00,0x08,0x00,0x00,0x00,0x3E,0x00,0x7F,0x00,0x00,
    ])),
    (16, "ICON_LIGHTNING", 1, bytes([
        0x00,0x00,0x80,0x00,0x80,0x00,0x80,0x01,0x80,0x03,0x00,0x07,0x00,0x0E,0xF8,0x0F,
        0xF8,0x07,0x00,0x03,0x00,0x07,0x80,0x06,0x80,0x04,0x80,0x00,0x80,0x00,0x00,0x00,
    ])),
    (17, "ICON_TEMP", 1, bytes([
        0x00,0x00,0xC0,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,
        0x40,0x01,0x40,0x01,0x60,0x03,0x60,0x03,0x60,0x03,0x60,0x03,0xC0,0x01,0x00,0x00,
    ])),
    (18, "ICON_FAN", 1, bytes([
        0x00,0x00,0xE0,0x03,0x18,0x0C,0x04,0x10,0xC2,0x21,0x22,0x22,0x1C,0x1C,0xF0,0x07,
        0xF0,0x07,0x1C,0x1C,0x22,0x22,0xC2,0x21,0x04,0x10,0x18,0x0C,0xE0,0x03,0x00,0x00,
    ])),
    (19, "ICON_LOCK", 1, bytes([
        0x00,0x00,0x00,0x00,0xE0,0x03,0x10,0x04,0x10,0x04,0x10,0x04,0xFE,0x3F,0x01,0x40,
        0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0xFE,0x3F,0x00,0x00,0x00,0x00,
    ])),
    (20, "ICON_HOME", 1, bytes([
        0x00,0x00,0x80,0x00,0xC0,0x01,0x60,0x03,0x30,0x06,0x18,0x0C,0x0C,0x18,0xFC,0x1F,
        0x0C,0x18,0x0C,0x18,0x0C,0x18,0x0C,0x18,0x0C,0x18,0xFC,0x1F,0x00,0x00,0x00,0x00,
    ])),
    (21, "ICON_GEAR", 1, bytes([
        0x00,0x00,0xC0,0x01,0x60,0x03,0x70,0x07,0x9E,0x3C,0x08,0x08,0x18,0x0C,0x30,0x06,
        0x30,0x06,0x18,0x0C,0x08,0x08,0x9E,0x3C,0x70,0x07,0x60,0x03,0xC0,0x01,0x00,0x00,
    ])),
    (22, "ICON_REFRESH", 1, bytes([
        0x00,0x00,0x1E,0x00,0x21,0x30,0x40,0x18,0x80,0x0C,0x80,0x07,0x80,0x01,0xC0,0x00,
        0xC0,0x00,0x80,0x01,0x80,0x07,0x80,0x0C,0x40,0x18,0x21,0x30,0x1E,0x00,0x00,0x00,
    ])),
    (23, "ICON_ARROW_UP", 1, bytes([
        0x00,0x00,0x80,0x00,0xC0,0x01,0xE0,0x03,0x70,0x07,0x38,0x0E,0x1C,0x1C,0x0E,0x38,
        0x04,0x10,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
    ])),
    (24, "ICON_ARROW_DN", 1, bytes([
        0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x04,0x10,
        0x0E,0x38,0x1C,0x1C,0x38,0x0E,0x70,0x07,0xE0,0x03,0xC0,0x01,0x80,0x00,0x00,0x00,
    ])),
    (25, "ICON_ARROW_LT", 1, bytes([
        0x00,0x00,0x00,0x00,0x00,0x00,0x80,0x00,0x40,0x00,0x20,0x00,0x10,0x00,0xF8,0x0F,
        0xF8,0x0F,0x10,0x00,0x20,0x00,0x40,0x00,0x80,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
    ])),
    (26, "ICON_ARROW_RT", 1, bytes([
        0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x01,0x00,0x02,0x00,0x04,0x00,0x08,0xF0,0x1F,
        0xF0,0x1F,0x00,0x08,0x00,0x04,0x00,0x02,0x00,0x01,0x00,0x00,0x00,0x00,0x00,0x00,
    ])),
    (27, "ICON_SIGNAL", 1, bytes([
        0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x08,0x00,0x1C,0x00,0x1C,
        0x00,0x08,0x80,0x08,0xC0,0x08,0xC0,0x08,0xE0,0x08,0xE0,0x09,0xE0,0x0B,0x00,0x00,
    ])),
    (28, "ICON_GLOBE", 1, bytes([
        0x00,0x00,0xE0,0x03,0x18,0x0C,0xC4,0x11,0x24,0x12,0x24,0x12,0xE4,0x13,0x24,0x12,
        0x24,0x12,0xE4,0x13,0x24,0x12,0x24,0x12,0xC4,0x11,0x18,0x0C,0xE0,0x03,0x00,0x00,
    ])),
    (29, "ICON_CHART", 1, bytes([
        0x00,0x00,0x00,0x00,0x00,0x08,0x00,0x08,0x00,0x08,0x00,0x08,0x00,0x2A,0x80,0x2A,
        0x80,0x2A,0x40,0x49,0x40,0x49,0x20,0x88,0x20,0x88,0x1C,0x88,0x00,0x00,0x00,0x00,
    ])),
    (30, "ICON_CLOCK", 1, bytes([
        0x00,0x00,0xE0,0x03,0x18,0x0C,0x04,0x10,0x84,0x10,0x84,0x10,0x84,0x13,0x84,0x10,
        0x84,0x10,0x84,0x10,0x44,0x10,0x24,0x10,0x04,0x10,0x18,0x0C,0xE0,0x03,0x00,0x00,
    ])),
```

- [ ] **Step 2: 更新 `build_icon_table()` 中的 `n_entries` 引用（自动从 len(ICON_SPEC) 推导，无需改动）**

验证: `build_icon_table()` 使用 `len(ICON_SPEC)` 计算 `n_entries`，无需手动改。

- [ ] **Step 3: 运行 generate_font.py 验证图标追加正确**

```bash
cd ch341
python generate_font.py
```

预期: `Icons: 0x000700` 区域大小从 1792B 增加到 ~2432B（+640B）

- [ ] **Step 4: 提交**

```bash
git add ch341/generate_font.py
git commit -m "feat: 新增 20 个功能图标到 ICON_SPEC (电池/警告/箭头/齿轮等, +640B)"
```

---

### Task 2: B — STM32 端图标渲染 (片内 ROM 回退 + Flash 查表)

**Files:**
- Modify: `Keil_Project/Hardware/TFT_Font_Data.h` (追加 20 个图标字模，作为 Flash 不可用时的回退)
- Modify: `Keil_Project/Hardware/Tft_Driver.h` (新增 `Tft_Driver_Draw_Icon_By_Id` 声明)
- Modify: `Keil_Project/Hardware/Tft_Driver.c` (新增 `Tft_Driver_Draw_Icon_By_Id` 实现)

- [ ] **Step 1: 在 `TFT_Font_Data.h` 末尾 (`#endif` 之前) 追加 20 个 ROM 回退图标**

```c
/* ═══════════════════════════════════════════════════════════════
 *  8. V4.3.1 新增 20 功能图标 — Flash 无效时的片内 ROM 回退
 * ═══════════════════════════════════════════════════════════════ */

static const uint8_t ICON_BATTERY[32] = {
    0x00,0x00,0x00,0x00,0xFE,0x3F,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,
    0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0xFE,0x3F,0x00,0x00,0x00,0x00,
};
static const uint8_t ICON_WARNING[32] = {
    0x00,0x00,0x00,0x00,0x80,0x00,0xC0,0x01,0x40,0x01,0x60,0x03,0x20,0x02,0x30,0x06,
    0x10,0x04,0x18,0x0C,0x10,0x04,0x20,0x02,0x20,0x02,0xC0,0x01,0x80,0x00,0x00,0x00,
};
static const uint8_t ICON_CHECK[32] = {
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x40,0x80,0x20,
    0xC0,0x11,0x60,0x0B,0x30,0x06,0x18,0x0C,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
};
static const uint8_t ICON_CROSS[32] = {
    0x00,0x00,0x00,0x00,0x00,0x00,0x02,0x20,0x06,0x30,0x0C,0x18,0x18,0x0C,0x30,0x06,
    0x60,0x03,0xC0,0x01,0xC0,0x01,0x60,0x03,0x30,0x06,0x18,0x0C,0x00,0x00,0x00,0x00,
};
static const uint8_t ICON_POWER[32] = {
    0x00,0x00,0x80,0x00,0x80,0x00,0x80,0x00,0x80,0x08,0x80,0x04,0x80,0x02,0x80,0x01,
    0x00,0x01,0x00,0x02,0x00,0x04,0x00,0x08,0x00,0x00,0x00,0x3E,0x00,0x7F,0x00,0x00,
};
static const uint8_t ICON_LIGHTNING[32] = {
    0x00,0x00,0x80,0x00,0x80,0x00,0x80,0x01,0x80,0x03,0x00,0x07,0x00,0x0E,0xF8,0x0F,
    0xF8,0x07,0x00,0x03,0x00,0x07,0x80,0x06,0x80,0x04,0x80,0x00,0x80,0x00,0x00,0x00,
};
static const uint8_t ICON_TEMP[32] = {
    0x00,0x00,0xC0,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,
    0x40,0x01,0x40,0x01,0x60,0x03,0x60,0x03,0x60,0x03,0x60,0x03,0xC0,0x01,0x00,0x00,
};
static const uint8_t ICON_FAN[32] = {
    0x00,0x00,0xE0,0x03,0x18,0x0C,0x04,0x10,0xC2,0x21,0x22,0x22,0x1C,0x1C,0xF0,0x07,
    0xF0,0x07,0x1C,0x1C,0x22,0x22,0xC2,0x21,0x04,0x10,0x18,0x0C,0xE0,0x03,0x00,0x00,
};
static const uint8_t ICON_LOCK[32] = {
    0x00,0x00,0x00,0x00,0xE0,0x03,0x10,0x04,0x10,0x04,0x10,0x04,0xFE,0x3F,0x01,0x40,
    0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0x01,0x40,0xFE,0x3F,0x00,0x00,0x00,0x00,
};
static const uint8_t ICON_HOME[32] = {
    0x00,0x00,0x80,0x00,0xC0,0x01,0x60,0x03,0x30,0x06,0x18,0x0C,0x0C,0x18,0xFC,0x1F,
    0x0C,0x18,0x0C,0x18,0x0C,0x18,0x0C,0x18,0x0C,0x18,0xFC,0x1F,0x00,0x00,0x00,0x00,
};
static const uint8_t ICON_GEAR[32] = {
    0x00,0x00,0xC0,0x01,0x60,0x03,0x70,0x07,0x9E,0x3C,0x08,0x08,0x18,0x0C,0x30,0x06,
    0x30,0x06,0x18,0x0C,0x08,0x08,0x9E,0x3C,0x70,0x07,0x60,0x03,0xC0,0x01,0x00,0x00,
};
static const uint8_t ICON_REFRESH[32] = {
    0x00,0x00,0x1E,0x00,0x21,0x30,0x40,0x18,0x80,0x0C,0x80,0x07,0x80,0x01,0xC0,0x00,
    0xC0,0x00,0x80,0x01,0x80,0x07,0x80,0x0C,0x40,0x18,0x21,0x30,0x1E,0x00,0x00,0x00,
};
static const uint8_t ICON_ARROW_UP[32] = {
    0x00,0x00,0x80,0x00,0xC0,0x01,0xE0,0x03,0x70,0x07,0x38,0x0E,0x1C,0x1C,0x0E,0x38,
    0x04,0x10,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
};
static const uint8_t ICON_ARROW_DN[32] = {
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x04,0x10,
    0x0E,0x38,0x1C,0x1C,0x38,0x0E,0x70,0x07,0xE0,0x03,0xC0,0x01,0x80,0x00,0x00,0x00,
};
static const uint8_t ICON_ARROW_LT[32] = {
    0x00,0x00,0x00,0x00,0x00,0x00,0x80,0x00,0x40,0x00,0x20,0x00,0x10,0x00,0xF8,0x0F,
    0xF8,0x0F,0x10,0x00,0x20,0x00,0x40,0x00,0x80,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
};
static const uint8_t ICON_ARROW_RT[32] = {
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x01,0x00,0x02,0x00,0x04,0x00,0x08,0xF0,0x1F,
    0xF0,0x1F,0x00,0x08,0x00,0x04,0x00,0x02,0x00,0x01,0x00,0x00,0x00,0x00,0x00,0x00,
};
static const uint8_t ICON_SIGNAL[32] = {
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x08,0x00,0x1C,0x00,0x1C,
    0x00,0x08,0x80,0x08,0xC0,0x08,0xC0,0x08,0xE0,0x08,0xE0,0x09,0xE0,0x0B,0x00,0x00,
};
static const uint8_t ICON_GLOBE[32] = {
    0x00,0x00,0xE0,0x03,0x18,0x0C,0xC4,0x11,0x24,0x12,0x24,0x12,0xE4,0x13,0x24,0x12,
    0x24,0x12,0xE4,0x13,0x24,0x12,0x24,0x12,0xC4,0x11,0x18,0x0C,0xE0,0x03,0x00,0x00,
};
static const uint8_t ICON_CHART[32] = {
    0x00,0x00,0x00,0x00,0x00,0x08,0x00,0x08,0x00,0x08,0x00,0x08,0x00,0x2A,0x80,0x2A,
    0x80,0x2A,0x40,0x49,0x40,0x49,0x20,0x88,0x20,0x88,0x1C,0x88,0x00,0x00,0x00,0x00,
};
static const uint8_t ICON_CLOCK[32] = {
    0x00,0x00,0xE0,0x03,0x18,0x0C,0x04,0x10,0x84,0x10,0x84,0x10,0x84,0x13,0x84,0x10,
    0x84,0x10,0x84,0x10,0x44,0x10,0x24,0x10,0x04,0x10,0x18,0x0C,0xE0,0x03,0x00,0x00,
};
```

- [ ] **Step 2: 在 `Tft_Driver.h` 末尾 (`#endif` 之前) 新增 API 声明**

```c
/** @brief 按 icon_id 绘制 16×16 图标 (Flash Icon Table / ROM 回退自动选择) */
void Tft_Driver_Draw_Icon_By_Id(uint16_t x, uint16_t y, uint8_t icon_id,
                                 uint16_t fg, uint16_t bg);
```

- [ ] **Step 3: 在 `Tft_Driver.c` 末尾追加 `Tft_Driver_Draw_Icon_By_Id` 实现**

```c
/* ═══════════════════════════════════════════════════════════════
 *  Icon By ID — Flash Icon Table 查表 / ROM 回退
 * ═══════════════════════════════════════════════════════════════ */

void Tft_Driver_Draw_Icon_By_Id(uint16_t x, uint16_t y, uint8_t icon_id,
                                 uint16_t fg, uint16_t bg)
{
    uint8_t row; uint16_t* p;
    SetWin(x, y, x + 15, y + 15);
    p = s_dma_buf;
    /* ── ROM 回退: 直接读 static 数组 (TFT_Font_Data.h) ── */
    switch (icon_id) {
        case 11: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_BATTERY[row*2],    ICON_BATTERY[row*2+1],    fg, bg, p + row*16); break;
        case 12: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_WARNING[row*2],    ICON_WARNING[row*2+1],    fg, bg, p + row*16); break;
        case 13: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_CHECK[row*2],      ICON_CHECK[row*2+1],      fg, bg, p + row*16); break;
        case 14: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_CROSS[row*2],      ICON_CROSS[row*2+1],      fg, bg, p + row*16); break;
        case 15: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_POWER[row*2],      ICON_POWER[row*2+1],      fg, bg, p + row*16); break;
        case 16: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_LIGHTNING[row*2],  ICON_LIGHTNING[row*2+1],  fg, bg, p + row*16); break;
        case 17: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_TEMP[row*2],       ICON_TEMP[row*2+1],       fg, bg, p + row*16); break;
        case 18: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_FAN[row*2],        ICON_FAN[row*2+1],        fg, bg, p + row*16); break;
        case 19: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_LOCK[row*2],       ICON_LOCK[row*2+1],       fg, bg, p + row*16); break;
        case 20: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_HOME[row*2],       ICON_HOME[row*2+1],       fg, bg, p + row*16); break;
        case 21: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_GEAR[row*2],       ICON_GEAR[row*2+1],       fg, bg, p + row*16); break;
        case 22: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_REFRESH[row*2],    ICON_REFRESH[row*2+1],    fg, bg, p + row*16); break;
        case 23: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_ARROW_UP[row*2],   ICON_ARROW_UP[row*2+1],   fg, bg, p + row*16); break;
        case 24: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_ARROW_DN[row*2],   ICON_ARROW_DN[row*2+1],   fg, bg, p + row*16); break;
        case 25: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_ARROW_LT[row*2],   ICON_ARROW_LT[row*2+1],   fg, bg, p + row*16); break;
        case 26: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_ARROW_RT[row*2],   ICON_ARROW_RT[row*2+1],   fg, bg, p + row*16); break;
        case 27: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_SIGNAL[row*2],     ICON_SIGNAL[row*2+1],     fg, bg, p + row*16); break;
        case 28: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_GLOBE[row*2],      ICON_GLOBE[row*2+1],      fg, bg, p + row*16); break;
        case 29: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_CHART[row*2],      ICON_CHART[row*2+1],      fg, bg, p + row*16); break;
        case 30: for (row = 0; row < 16; row++) Decode_CN_Row(ICON_CLOCK[row*2],      ICON_CLOCK[row*2+1],      fg, bg, p + row*16); break;
        default: for (row = 0; row < 16; row++) p[row*16] = p[row*16+1] = p[row*16+2] = p[row*16+3] = bg; break; /* unknown → 黑块 */
    }
    Tft_DMA_Send(s_dma_buf, 256);
}
```

> 注意: Flash 路径 (g_font_flash_valid==1 时通过 W25Q 读取 Icon Table) 留作后续 Task B-flash 补充——当前 ROM 回退路径足够所有现有 UI 场景使用。

- [ ] **Step 4: Keil 构建验证**

Keil → Rebuild all (F7)
Expected: `0 Error(s), 0 Warning(s)`

- [ ] **Step 5: 提交**

```bash
git add Keil_Project/Hardware/TFT_Font_Data.h Keil_Project/Hardware/Tft_Driver.h Keil_Project/Hardware/Tft_Driver.c
git commit -m "feat: 新增 Tft_Driver_Draw_Icon_By_Id + 20 个 ROM 回退图标 (电池/警告/箭头/齿轮等)"
```

---

### Task 3: A — 开机动画 Python 生成器 + 烧录器

**Files:**
- Create: `ch341/generate_splash.py`
- Create: `ch341/burn_splash.py`

- [ ] **Step 1: 创建 `ch341/generate_splash.py`**

```python
#!/usr/bin/env python3
"""
generate_splash.py — 生成 WPT-PWM 开机动画 splash.bin (5帧 fade-in)
V1.0  2026-06-24

输出: splash.bin (200KB, 5帧 × 40KB 160x128 RGB565)
SPLASH Header: magic="SP"(0x5350) + version + n_frames + width + height + frame_bytes
"""

import struct, os
from PIL import Image, ImageDraw, ImageFont

SPLASH_BIN   = os.path.join(os.path.dirname(__file__), "splash.bin")
W, H         = 160, 128
N_FRAMES     = 5
FRAME_BYTES  = W * H * 2  # 40960
MAGIC        = 0x5350      # "SP"

def rgb888_to_rgb565(r, g, b):
    return ((r >> 3) << 11) | ((g >> 2) << 5) | (b >> 3)

def render_frame(brightness: float) -> bytes:
    """渲染一帧 160×128 RGB565, brightness 0.0~1.0"""
    img = Image.new('RGB', (W, H), (0, 0, 0)); draw = ImageDraw.Draw(img)
    try: font_title = ImageFont.truetype("simsun.ttc", 20)
    except: font_title = ImageFont.load_default()
    try: font_sub = ImageFont.truetype("simsun.ttc", 14)
    except: font_sub = ImageFont.load_default()

    # 标题
    title = "WPT-PWM"
    bbox = draw.textbbox((0, 0), title, font=font_title)
    tw = bbox[2] - bbox[0]
    draw.text(((W - tw)//2, 25), title, font=font_title,
              fill=(int(255*brightness), int(200*brightness), 0))

    # 副标题
    sub = "Wireless Power Transfer"
    bbox2 = draw.textbbox((0, 0), sub, font=font_sub)
    sw = bbox2[2] - bbox2[0]
    draw.text(((W - sw)//2, 52), sub, font=font_sub,
              fill=(int(180*brightness), int(180*brightness), int(180*brightness)))

    # 版本号
    ver = "V4.3.1"
    bbox3 = draw.textbbox((0, 0), ver, font=font_sub)
    vw = bbox3[2] - bbox3[0]
    draw.text(((W - vw)//2, 72), ver, font=font_sub,
              fill=(int(120*brightness), int(120*brightness), int(120*brightness)))

    # 底部提示
    hint = "STM32 + ESP8266 + OneNET"
    bbox4 = draw.textbbox((0, 0), hint, font=font_sub)
    hw = bbox4[2] - bbox4[0]
    draw.text(((W - hw)//2, 100), hint, font=font_sub,
              fill=(int(100*brightness), int(100*brightness), int(100*brightness)))

    # RGB888 → RGB565 逐像素转码
    frame = bytearray(FRAME_BYTES)
    for y in range(H):
        for x in range(W):
            r, g, b = img.getpixel((x, y))
            rgb565 = rgb888_to_rgb565(r, g, b)
            frame[(y*W + x)*2]     = rgb565 & 0xFF
            frame[(y*W + x)*2 + 1] = (rgb565 >> 8) & 0xFF
    return bytes(frame)

def main():
    buf = bytearray()
    # SPLASH Header (32B)
    buf.extend(struct.pack('<H', MAGIC))           # magic
    buf.append(1)                                    # version
    buf.append(N_FRAMES)                             # n_frames
    buf.extend(struct.pack('<HH', W, H))             # width, height
    buf.extend(struct.pack('<I', FRAME_BYTES))       # frame_bytes
    buf.extend(b'\x00' * 20)                         # reserved
    # 5 帧 fade-in
    for i in range(N_FRAMES):
        brightness = 0.2 + 0.8 * (i / max(N_FRAMES-1, 1))
        print(f"  Frame {i+1}/{N_FRAMES}: brightness={brightness:.1f}")
        buf.extend(render_frame(brightness))
    with open(SPLASH_BIN, 'wb') as f:
        f.write(buf)
    print(f"[OK] 生成 {SPLASH_BIN} ({len(buf)} 字节 = {len(buf)//1024}KB)")

if __name__ == '__main__':
    main()
```

- [ ] **Step 2: 运行 generate_splash.py 生成 splash.bin**

```bash
cd ch341
python generate_splash.py
```

预期: `[OK] 生成 splash.bin (200000 字节 = 195KB)`

- [ ] **Step 3: 创建 `ch341/burn_splash.py`**

```python
#!/usr/bin/env python3
"""burn_splash.py — 烧录开机动画到 W25Q128 SPLASH 分区 (0x200000, 1MB)"""

import subprocess, os, sys

SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))
SPLASH_BIN = os.path.join(SCRIPT_DIR, "splash.bin")
FLASHROM   = os.path.join(SCRIPT_DIR, "flashrom-1.4", "flashrom.exe")
if not os.path.exists(FLASHROM): FLASHROM = "flashrom"
NO_WIN   = subprocess.CREATE_NO_WINDOW if sys.platform == "win32" else 0
SPLASH_ADDR = 0x200000

def run(cmd, desc):
    print(f"[..] {desc}")
    r = subprocess.run(cmd, capture_output=True, text=True, timeout=120, creationflags=NO_WIN)
    if r.returncode != 0: print(r.stdout + r.stderr); sys.exit(1)
    print(f"[OK] {desc}")

def main():
    if not os.path.exists(SPLASH_BIN):
        print("请先运行 generate_splash.py")
        sys.exit(1)
    size = os.path.getsize(SPLASH_BIN)
    if size > 1*1024*1024: print(f"SPLASH 大小 {size} > 1MB!"); sys.exit(1)

    # 备份全片
    backup = os.path.join(SCRIPT_DIR, "splash_backup.bin")
    print("[Step 1/3] 备份全片...")
    run([FLASHROM, "-p", "ch341a_spi", "-r", backup, "-c", "W25Q128.V"], "备份")

    # 读入备份 → 覆盖 SPLASH 区域 → 写回
    fb = bytearray(open(backup, "rb").read())
    spl = open(SPLASH_BIN, "rb").read()
    fb[SPLASH_ADDR : SPLASH_ADDR + len(spl)] = spl
    merged = os.path.join(SCRIPT_DIR, "splash_merged.bin")
    open(merged, "wb").write(fb)

    print("[Step 2/3] 写入 SPLASH 分区...")
    run([FLASHROM, "-p", "ch341a_spi", "-w", merged, "-c", "W25Q128.V"], "写入")

    # 校验
    print("[Step 3/3] 读回校验...")
    run([FLASHROM, "-p", "ch341a_spi", "-r", backup, "-c", "W25Q128.V"], "读回")
    vb = open(backup, "rb").read()
    err = sum(1 for i in range(SPLASH_ADDR, SPLASH_ADDR+len(spl)) if vb[i] != fb[i])
    if err:
        print(f"[FAIL] {err} 字节不一致!"); sys.exit(1)
    print("[PASS] SPLASH 烧录成功!")

    os.remove(merged)
    os.remove(backup)

if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 提交**

```bash
git add ch341/generate_splash.py ch341/burn_splash.py
git commit -m "feat: 开机动画 Python 生成器 + 烧录器 (5帧 fade-in → SPLASH 分区)"
```

---

### Task 4: A — STM32 端开机动画显示

**Files:**
- Modify: `Keil_Project/User/Sys_Core.c` (Sys_Startup_Screen 改造)
- Modify: `Keil_Project/Hardware/W25Q_Driver.h` (SPLASH_MAGIC 宏)

- [ ] **Step 1: 在 `W25Q_Driver.h` 追加 SPLASH 宏**

```c
#define SPLASH_MAGIC       0x5350U  /* "SP" */
#define SPLASH_BASE        W25Q_ADDR_SPLASH  /* 0x200000 */
```

- [ ] **Step 2: 在 `Sys_Core.c` 顶部追加外部声明 + splash 静态变量**

```c
#include "W25Q_Driver.h"     /* SPLASH_MAGIC, W25Q_Driver_Read */
#include "Key_Driver.h"      /* Key_Driver_Any_Pressed */

static uint8_t s_splash_valid = 0;       /* SPLASH 分区有效标志 */
```

- [ ] **Step 3: 新增 `Splash_Check()` 函数 (在 `Sys_Post_Init()` 之前)**

```c
/** @brief 上电阶段检查 SPLASH 分区头部, 烧录后置有效标志 */
static void Splash_Check(void)
{
    uint8_t hdr[4];
    W25Q_Driver_Read(SPLASH_BASE, hdr, 4);
    if (hdr[0] == 0x50 && hdr[1] == 0x53) {  /* "SP" LE */
        s_splash_valid = 1;
    }
}
```

- [ ] **Step 4: 改写 `Sys_Startup_Screen()`**

```c
void Sys_Startup_Screen(void)
{
    uint8_t frame;
    Tft_Driver_Clear(TFT_COLOR_BLACK);

    /* 检测 SPLASH 分区: W25Q_Driver_Init 先于本函数, 已就绪 */
    Splash_Check();

    if (s_splash_valid) {
        uint16_t row; uint32_t frame_base;
        /* 分 8 次 DMA 泵送全屏 160×128 = 20480 像素 (每次 2560 像素) */
        uint16_t buf[256];
        for (frame = 0; frame < 5; frame++) {
            frame_base = SPLASH_BASE + 32 + (uint32_t)frame * 40960;  /* skip 32B header */
            SetWin(0, 0, 159, 127);
            Tft_SPI_16bit();
            for (row = 0; row < 80; row++) {  /* 80 次 DMA = 20480 像素 */
                uint16_t i; uint16_t* p;
                W25Q_Driver_Read(frame_base + (uint32_t)row * 512, (uint8_t*)buf, 512);
                /* 256 像素 × 80 次 = 20480 = 全屏 */
                Tft_DMA_Transfer(buf, 256, 1);
            }
            while (SPI_I2S_GetFlagStatus(SPI1, SPI_I2S_FLAG_BSY) == SET);
            Tft_SPI_8bit();
            Sys_Timer_Delay_Ms(50);
            if (Key_Driver_Any_Pressed()) break;
        }
    } else {
        /* 回退 → 纯文本启动画面 */
        Tft_Driver_Show_CN_String(3, 3, "WPT-PWM", TFT_COLOR_YELLOW, TFT_COLOR_BLACK);
        Tft_Driver_Show_CN_String(5, 3, "\xe5\x90\xaf\xe5\x8a\xa8\xe4\xb8\xad" "...",
                                  TFT_COLOR_WHITE, TFT_COLOR_BLACK);
    }
    Tft_Driver_Set_Backlight(255);
}
```

- [ ] **Step 5: Keil 构建验证 + 提交**

Keil → F7 Rebuild. Expected: 0E0W.

```bash
git add Keil_Project/User/Sys_Core.c Keil_Project/Hardware/W25Q_Driver.h
git commit -m "feat: Sys_Startup_Screen SPLASH 动画播放 (5帧 fade-in, DMA 分8次泵送)"
```

---

### Task 5: C — 12×12 CJK 小字库 (Python 生成 + STM32 解码)

**Files:**
- Modify: `ch341/generate_font.py` (新增 12×12 字模生成 + Font_Header v2 扩展)
- Modify: `Keil_Project/Hardware/Tft_Driver.h` (新增 12×12 API)
- Modify: `Keil_Project/Hardware/Tft_Driver.c` (新增 12×12 解码 + Flash 读取)
- Modify: `Keil_Project/Hardware/W25Q_Driver.h` (Font_Header v2 扩展字段)

> ⚠️ 注: Task 5 为可选，如果 FONT 分区空间紧张可延后。优先级为 ③。

---

### Task 6: 文档更新 + 最终构建

**Files:**
- Modify: `ch341/README.md` (追加 splash + icon 操作章节)
- Modify: `CLAUDE.md` (版本 V4.3.1 → V4.3.2, 审查历史追加)

- [ ] **Step 1: 更新 README.md 新增 splash 烧录步骤**

```markdown
### 7.6 烧录开机动画 (可选)

```bash
python ch341/generate_splash.py   # 渲染 → splash.bin (200KB)
python ch341/burn_splash.py       # 烧录 → W25Q128 SPLASH 分区 0x200000
```
```

- [ ] **Step 2: 更新 CLAUDE.md**

版本: V4.3.1 → V4.3.2
审查历史追加: `| V4.3.2 | 字库扩展: 开机动画(SPLASH 5帧 fade-in) + 20新图标 + 12×12小字库 |`

- [ ] **Step 3: 全量编译 + 最终提交**

```bash
cmd.exe /c Keil_Project\keilkill.bat
# Keil F7 Rebuild → 0E0W
git add -A
git commit -m "feat: V4.3.2 — 字库扩展: 开机动画 + 新图标 + 12x12小字库"
git push origin 4.0TFT
```
